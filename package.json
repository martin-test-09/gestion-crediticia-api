{
  "name": "credit-api",
  "version": "1.0.0",
  "description": "API de Gestión Crediticia para presupuestos de crédito automotor.",
  "packageManager": "pnpm@11.5.2",
  "type": "module",
  "main": "src/main.js",
  "scripts": {
    "preinstall": "node -e \"const ua=process.env.npm_config_user_agent||''; if (!ua.startsWith('pnpm/')) { console.error('Este proyecto usa pnpm. Ejecuta: pnpm install'); process.exit(1); }\"",
    "dev": "nodemon src/main.js",
    "start": "node src/main.js",
    "seed": "node src/seed/seed.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "bcrypt": "^6.0.0",
    "cors": "^2.8.5",
    "dotenv": "^17.2.3",
    "express": "^5.1.0",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^9.0.0",
    "nodemailer": "^9.0.3"
  },
  "devDependencies": {
    "nodemon": "^3.1.10"
  }
}
